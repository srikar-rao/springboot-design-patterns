package com.dev.org.export.model;

/**
 * Enum representing the types of exporters available in the system.
 * Used to select the appropriate exporter implementation.
 */
public enum ExporterType {
    PROJECT,
    USER,
    TASK
}

package com.dev.org.export.model;

/**
 * Enum representing the file formats supported for export.
 * Used to select the appropriate file generator strategy.
 */
public enum FileFormat {
    CSV,
    EXCEL
}
